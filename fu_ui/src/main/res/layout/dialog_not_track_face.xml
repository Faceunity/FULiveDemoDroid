<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="245dp"
    android:layout_height="245dp"
    android:background="@drawable/bg_confirm_dialog">

    <ImageView
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_marginTop="40dp"
        android:src="@mipmap/icon_poster_no_track_face"
        android:layout_centerHorizontal="true"  />

    <TextView
        android:id="@+id/tv_tip_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="135dp"
        android:paddingLeft="8dp"
        android:paddingRight="8dp"
        android:text="@string/dialog_no_track_face"
        android:textColor="@color/black_dialog_text"
        android:textSize="14sp"
        android:layout_centerHorizontal="true" />

    <Button
        android:id="@+id/btn_done"
        android:layout_width="match_parent"
        android:layout_height="45dp"
        android:background="@drawable/bg_confirm_dialog_option"
        android:gravity="center"
        android:text="@string/dialog_got"
        android:textColor="@color/colorWhite"
        android:textSize="13sp"
        android:layout_centerHorizontal="true"
        android:layout_alignParentBottom="true"
        />

</RelativeLayout>